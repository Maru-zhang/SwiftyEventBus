target 'SwiftyEventBus' do
    use_frameworks!
    platform :ios, '8.0'
    pod 'RxSwift'
    target 'SwiftyEventBusTests' do
        inherit! :search_paths
        pod 'Quick'
        pod 'Nimble'
    end
end
